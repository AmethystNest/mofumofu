# 配給は一人分 — Claude Code 引き継ぎ書

スマホ向けの犬育成シミュレーション。舞台はディストピアの管理都市で、残されたのは主人公と犬だけ。
本書は claude.ai で作った簡易版プロトタイプ(`haikyu.html`)から、最高品質版へ作り直すための引き継ぎ書。

## 1. 確定事項（勝手に変更しない）

| 項目 | 決定内容 |
|---|---|
| 品質レベル | **現環境で実現できる最高品質**。「まず動けばよい」をユーザーの許可なく最終方針にしない。途中で制約が判明したら、制約・品質への影響・代替案をユーザーに報告して判断を仰ぐ。黙って簡易方式へ落とさない |
| 物語 | **B「管理都市の休日」**：住民の消えた都市でAI〈ミナト〉が動き続け、配給は「一人分」だけ届く。未登録の犬と分け合うこと自体が規則違反 |
| 犬の死 | **死なない**。放置しても衰弱・不安・信頼低下にとどめる |
| 画面・言語 | スマホ縦画面、日本語 |
| 配布 | 友人への直接配布。PWAとしてGitHub Pagesでホスティング（ストア配布なし） |
| 対応環境 | iPhone Safari / iOS PWA（最優先）と Android Chrome。両対応コードを先に実装し、実機確認は最終段階で行う。**ユーザーの手元の実機は Android のみ（iPhone なし、2026-09-25 確認）** |
| 時間進行 | 1日はプレイヤーの操作で進む（3行動 → 眠る）。実時間での減衰はしない。不在時は罰を与えず「犬が待っていた」演出のみ |
| 犬の見た目 | **2D・黒柴（毛色は純黒。赤茶の部分は控えめで黒寄り）**。画風の基準は `docs/reference/shiba-style.png`。口は犬らしく（鼻下の縦線から「人」の字、開くと下あごが下がり舌が垂れる）（2026-09-25 ユーザー指定） |
| 追加要素 | **子犬から成長する**（子犬→若犬→成犬）、**着せ替え**、**部屋のデコレーション**（2026-09-25 ユーザー要望）。**初期装備はなし** |

## 2. 現状：簡易版プロトタイプ `haikyu.html`

依存なしの単一HTML（素のJS + SVGの犬 + DOM UI + localStorage）。**仕様と物語の叩き台として扱い、コードは移植元として参照する。**

### 実装済みの内容
- 1日3行動（なでるは1日3回まで・ごはんは行動を消費しない / 遊ぶ＝1 / 探しに行く＝2）→ 夜イベント → 朝の配給。
- 犬のステータス：おなか・げんき・信頼・不安（UIでは「安心」＝100−不安）。主人公のステータス：体力（0になると倒れ、翌日の行動が1になる）。
- 探索先：旧商店街 / 地下駐車場 / 管理塔の外周（地図で解放、市民登録すると閉鎖）。犬の嗅覚で追加発見、信頼が高いと巡回ドローンを警告してくれる。
- 第1章（7夜）：n1 寝床、n2 夜間放送、n3 首輪の札、n4 消費量の不一致、n5 雷、n6 話、ch1 市民登録の選択（登録すると配給が2人分・塔へ行けない / 未登録なら信頼+10）。エンディング文は信頼70以上か未満かで分岐。その後は日替わりの小話。
- 設定：サウンドのオン/オフ、セーブの書き出し/読み込み（base64）、初期化。
- ゲームルールは `/*CORE-START*/ … /*CORE-END*/` 内の純粋ロジックに分離済み（シード付き乱数）。

### 検証済み（Chromiumエミュレーションのみ）
- 20日×600回のシミュレーション：値の範囲外・NaNなし、日付の進行とセーブの往復は正常。
- バランス：丁寧に世話すると7日目の信頼は平均92〜99、雑な世話だと約47。
- 390px / 320px幅、ライト/ダークで、タイトル→本編→食事→探索→夜→第1章→リロード継続まで動作確認済み。横スクロールなし。

### 未検証・既知の課題
- **iPhone / Android 実機では未確認**（SafariでのSVG transform-originの挙動、iOSの音声アンロックを含む）。iPhone実機で確認する手段の確保が必要。
- 眠り姿勢のとき首輪の線が顔に掛かる。
- 信頼が第1章の後で上限に張り付き、成長目標がなくなる。
- 第1章の後は食料が余りやすい。
- 音は単純なサイン波だけ。
- 振動はAndroidのみ（iOS Safariは非対応の見込み）。
- 保存は localStorage のみ。

## 3. 最高品質版の技術方針（案。★は実装前に公式仕様または実挙動で確認すること）

- **構成：** TypeScript + Vite。PWA対応（Service Worker・manifest。★vite-plugin-pwa 等の採用可否を確認）。
- **描画：** PixiJS v8（部屋・天候・光・犬）と、DOMのUI（日本語テキスト・メニュー・アクセシビリティ）の二層構成。
- **物語：** ink / inkjs で分岐・変数・条件を管理する（★ライセンスとiOS上の動作を確認）。
- **保存：** IndexedDB にスキーマのバージョン管理と移行処理を付け、書き出しによるバックアップも用意する。`navigator.storage.persist()` も併用する（★iOSでのデータ消去条件を確認）。
- **音：** Howler.js か Web Audio。BGM・環境音・犬の声を用意し、iOSでは最初のタップで音声をアンロックする。
- **テスト：** Vitest（core）、Playwright（モバイルエミュレーション・スクリーンショット比較）、Lighthouse CI、axe-core。
- **最大のボトルネック＝犬の見た目とアニメーション品質。** 最初にここを比較して決める。
  1. **Blenderで3Dの犬を作り、プリレンダーしてスプライトシート化**（Claude Codeからスクリプト操作できる。★モデルのライセンスを確認）。
  2. **2Dボーンアニメーション**（★Spine・Rive等の料金・ランタイムライセンス・iOSでの性能を確認）。
  3. **手描き / AI生成の2D**（コマ間の一貫性が課題）。

  → 待機・なでられ反応・食事・眠り・しょんぼりの5状態で試作し、見た目・容量・iPhoneでの性能を比べてユーザーに提示し、選んでもらう。

### ディレクトリ構成（案）
```
src/core/      純TS：ルール・時間進行・イベント判定・乱数（描画非依存、Vitestで全網羅）
src/content/   ink物語、探索地点・アイテム・台詞のデータ
src/render/    Pixiシーン、犬アニメーションの状態機械、天候・時間帯
src/ui/        DOM UI（シート・ログ・ステータス）
src/platform/  保存・音・入力・PWA・触覚
```
`core` は `haikyu.html` の CORE から移植し、数値は叩き台として引き継ぐ。

## 4. 開発順序（各段階で「完成の定義」を満たしてから次へ進む）

1. **環境構築と core の移植：** Vite+TS+PWAの雛形を作り、core を移植する。シミュレーションテストでプロトタイプと同じ挙動になることを確認する。
2. **犬の表現方式を比較（第3章の試作）：** ユーザーの承認を得る。
3. **縦切り版：** 部屋1画面・犬5状態・ごはん/なでる/遊ぶ・保存・PWAインストールまで。音と演出も最終品質の水準で作る。
4. **探索ループ：** 探索画面の演出、ドローン回避、持ち帰りの表示。
5. **ink の導入と第1章の移植・推敲。** 既知の課題（信頼の張り付き・食料の余り）はここでバランスを再設計する。
6. **第2章以降とエンディング**（死なない前提で、別れではない結末を複数用意）。
7. **品質検証：** 機能・見た目・音・操作性・性能（iPhoneで60fps目標）・安定性・アクセシビリティ。最後に iPhone / Android 実機で確認する。

## 5. 作業ルール
- 「変更した」と「正しく動作した」を区別して報告する。実行して確認したこと・静的に確認したこと・仕様から推定したことも区別して明示する。
- エラーなく動いただけで完成にしない。見た目はスクリーンショットで自己レビューする。
- 確定仕様・承認済みの素材は、必要がなければ触らない。
- 成果物はZIPにせず、ファイルのまま渡す。

## 6. 進捗記録

### 第1段階 環境構築と core の移植 — 完了（2026-09-25）
- 構成：Vite 8 / TypeScript 7 / Vitest 5 / vite-plugin-pwa 1.3（Vite 8 対応を peerDependencies で確認済み。いずれも MIT）。
- `prototype/haikyu.html`：移植元のプロトタイプ（参照用・変更しない）。
- `src/core/`：CORE を型付きで分割移植（types / rng / util / state / actions / night / time）。`src/content/`：品目・探索地点・台詞。
  - 夜イベントの本文は第5段階で ink に移すまで `src/core/night.ts` に置く。
  - プロトタイプからの意図的な差分は1点のみ：犬の名前に `$` を含むと `{name}` 置換が崩れる不具合を修正。
- 検証（実行確認）：
  - `tests/equivalence.test.ts`：プロトタイプの CORE を直接読み込み、300本×最大40日のランダム操作列（約5万ステップ、セーブ往復・不在復帰を含む）で戻り値と状態全体が毎ステップ一致。数値を1か所変えると失敗することも確認済み。
  - `tests/simulation.test.ts`：20日×600回で範囲外・NaNなし、日付進行とセーブ往復正常。7日目の信頼平均：丁寧 99.1。
- PWA：`BASE_PATH=/mofumofu/` でビルドし、Chromium（390px モバイルエミュレーション）で SW 登録・manifest・アイコン取得・オフライン再読み込みを確認。
  - 更新は `registerType: 'prompt'`（プレイ中に自動で再読み込みしない）。更新通知UIは第3段階で作る。
  - アイコンは仮（`npm run icons` で SVG から PNG を生成）。
- デプロイ：`.github/workflows/deploy.yml`（main への push でテスト→ビルド→Pages）。**リポジトリ設定で Pages のソースを「GitHub Actions」にする必要がある（未設定・未確認）。** Action のバージョンは既知の安定版を指定しており、最新版かは未確認。
- 環境メモ：Playwright は `/opt/pw-browsers/chromium` を executablePath で使う（`CHROMIUM_PATH` 環境変数）。download.blender.org はこの環境のネットワーク方針で遮断、PyPI（`bpy` ホイール）は到達可能。

### 第2段階 犬の表現方式の比較 — 決定：2D（2026-09-25）
- 3D→スプライト案・旧2D案を試作して比較（コードは履歴のコミット 8b43a2e に残る）。ユーザーの選択は **2D**。
- ユーザー提供の参考画像 `docs/reference/shiba-style.png` のテイストで描き直すよう指示あり（画風の基準）：
  ちびキャラ体型の黒柴、大きな頭と瞳、柔らかい炭色の黒・赤茶・クリーム、毛束の輪郭線、赤い唐草バンダナ。
- 現在の実装：`src/lab/shiba/`（`painter.ts` 毛束輪郭＋筆致の描画、`parts.ts` 参考画像座標のパーツ、`dog.ts` 骨組みと表情）。
  確認ページ `lab/dog.html`。起動時に Canvas2D で描画（約60ms・GPU 約1MB・画像ファイルなし）。
  5状態：待機（瞬き・首かしげ・耳ぴく）／なでられ（^^・舌・頬の赤み・跳ねる・速い尻尾）／食事（器・もぐもぐ）／眠り（伏せ）／しょんぼり（困り目・への字口・垂れ尾）。状態間はばねで補間。
- 未確認：実機（Android）での見た目・FPS。

### 公開ページの不具合（2026-09-25 修正）
- 第1段階の版の Service Worker が `lab/dog.html` への遷移をトップページに差し替え、しかも `prompt` 設定のため新版に切り替わらなかった。
- 対策：`navigateFallbackDenylist` で lab を除外、開発中は `registerType: 'autoUpdate'`（即時切替）。旧版が残っていても2回目の表示から正しく開くことを再現試験で確認。
- 第3段階で更新のお知らせUIを作り、`prompt` に戻す。

### 純黒・口元・成長・着せ替え・部屋デコ（2026-09-25）
- 純黒：地は #000。形はツヤの筆致（光の当たる左上ほど明るい）と縁の反射光で見せる（`painter.ts` の `sheen`）。瞳は淡い縁取りで黒い顔に沈まないようにした。
- 口：マズルのふくらみ（陰影のみ）＋「人」の字の口。開口時は下あごが下がり、舌が垂れる（`dog.ts` の `Mouth`）。
- 成長：`setAge(0..1)`（0=子犬・0.5=若犬・1=成犬）で頭身・脚・耳・目・鼻づら・全体の大きさを連続変化。
- 着せ替え：首（バンダナ4種：唐草赤・麻の葉藍・水玉山吹・チェック若草／首輪と札）、頭（ベレー帽・花飾り・リボン）。骨に追従し、成長しても位置が合う。
- 部屋：`src/lab/room/room.ts`。コンクリートの壁・窓の外の管理都市・木の床を同じ画風で描画。置き場所6つ（窓辺・壁・明かり・寝床・床・おもちゃ）に各2〜3種。明かりの種類で部屋の光の色が変わる。
- **ルール面は未決（案）**：成長は日数で進める（例：1〜7日目＝子犬／第1章、8〜21日目＝若犬、22日目〜＝成犬）。服や家具は探索で拾う・ミナトの配給に混ざる等で入手（既存の毛布・古いボールは寝床・おもちゃに対応）。第5段階のバランス再設計で決める。
- 未確認：実機（Android）での見た目・FPS。

### 画質の刷新・成犬の体型（2026-09-25、ユーザー指摘「少しチープ」「成犬のバランスが悪い」への対応）
- `painter.ts` を作り直し：形の内側の距離変換 → 丸みの高さ → 法線 → ピクセル単位の光（左上の光・ツヤ・縁の反射光・縁の暗がり）。
  輪郭は大小不揃いの毛束（細長い房を数本ずつ、流れの向きに傾ける）、線はその場所の色を暗くした色線。頭→胴、胴→脚の落ち影。
- 成長は拡大縮小ではなく「配置と形を段階ごとに決めて描き直す」方式（`parts.ts` の `layout(age)`）。
  脚は長さ・太さから形を作る（肉球は伸ばさない）、頭の輪郭は子犬形⇔成犬形を補間（頬が締まり、あごと鼻づらが下へ伸びる）、成犬は頭身0.84・耳1.26倍・全体サイズは子犬0.72→成犬1.0。
  伏せでは胴の底が床に着くまで下げ、食事では成犬ほど頭を器まで下げる。
- 初期装備はなし（`NO_OUTFIT`）。
- 描き直しの時間：PC の Chromium で約0.12秒（2倍解像度）〜0.28秒（3倍）。Android 中位機では数倍と推定（未計測）。
  **第3段階で対策する**：描いた結果の保存・再利用（IndexedDB）か、OffscreenCanvas＋Worker での描画。

### パーツの分離修正・着せ替えと部屋の質感・ツール選定（2026-09-25、ユーザー指摘「成犬になるにつれ耳等が分離」「着せ替え等も高品質を追究」「ツールを選定」）
- 耳：付け根を「その成長段階の頭の輪郭上の点」に追従させる（`earRoot(side, age)`）。付け根は頭の内側まで伸ばして隠す。
- 首まわり：結び目を「頭の座標で見たあごの下の点」に固定し、幅は頭の大きさに比例（全成長段階で同じ関係）。
- `tests/dog-parts.test.ts`：全成長段階（0.05刻み）で、耳の付け根（アニメの角度範囲込み）・尾の付け根・脚の上端・首まわりの帯の両端が、頭／胴の内側（毛束の長さ以内）にあることを検査。
- 着せ替え：`src/lab/shiba/cloth.ts`（模様タイル・曲線に沿った柄・しわ・布目・金属）と painter の縫い目（`stitch`）。
  バンダナは首のカーブに沿った柄・あご下の陰・横じわ・縫い目・結び目のしわ。首輪は革のしぼ・両縁のステッチ・真鍮のバックル・Dカン・擦れた刻印の札。
  ベレー帽はフェルトの質感と放射状のふくらみ・縫い目の帯、花は根元が色づくグラデーションと葉脈・しべ、リボンはサテンのツヤ・しわ・垂れ。
- 部屋：カーテン（布目・ひだ・タッセル・縫い目）、クッション（チェック柄・縫い目・ボタン）、毛布（織り・しわ）、ラグ（同心の織り・房飾り）、畳（い草・縁）、段ボール（波目・ガムテープ・「配給」の印）、鉢植え（葉脈）、ボール（縫い目）、ぬいぐるみ（縫い目）。

#### ツール選定（2026-09-25 決定）
| 用途 | 採用 | 理由 | 不採用とその理由 |
|---|---|---|---|
| 描画・アニメ | PixiJS v8（MIT） | WebGL で軽い。標準の MeshRope / MeshPlane で布や尾の変形も可能 | — |
| 絵づくり | 自作 `painter.ts`（Canvas2D＋ピクセル単位の陰影）＋ `cloth.ts` | コードで作れ、成長・着せ替えに合わせて描き直せる。画像ファイル不要 | Spine（編集がGUI専用・有料・ランタイムにも契約が必要）、Rive（編集がGUI専用でこちらから作れない） |
| 動きの補間 | 自作のばね（臨界減衰） | 状態間を自然につなげる。依存なし | GSAP（独自ライセンス。機能は足りている） |
| 形の検査 | Vitest（形の接合テスト） | 分離の再発を自動で検出 | — |
| 見た目の検査 | Playwright（Chromium・390px・2〜3倍） | スクリーンショットで自己レビュー | — |
| 保存（第3段階） | idb（ISC）＋ IndexedDB | 薄いラッパーで安全に扱える | localStorage のみ（容量・消去条件の不安） |
| 描画の裏スレッド化（第3段階） | comlink（Apache-2.0）＋ OffscreenCanvas | 成長時の描き直しで画面を止めない | — |
| 時間帯の色調（第3段階） | pixi-filters（MIT） | 朝〜夜の色味・明かりのにじみ | — |

### 顔の配置・赤茶の調整（2026-09-25、ユーザー指摘「目の位置と眉の模様の位置がずれている」「茶色部分を控えめ・黒寄りに」）
- 原因：参考画像の傾いた顔の座標を目・眉・鼻だけ写し、頬や白い口元の模様は傾きなしで描いていたため、目・眉・模様の関係がずれていた。
- `parts.ts` の `FACE`（顔の配置表）に目・眉・鼻・鼻づら・頬を集約し、頭に描く模様（頬の赤茶・白い口元・鼻筋）もそこから配置。
  目はほぼ水平、眉は目の真上（やや内側）、鼻は目から離した。
- `tests/dog-parts.test.ts` に「全成長段階で眉が目の真上・眉と目の間隔・鼻と目の距離・目が頭の内側・左右の目の高さ」の検査を追加（12件）。
- 赤茶：rgb(214,140,72) → rgb(142,78,38)、面積も縮小（頬は外側だけ、脚は下半分、耳・尾・胸の縁は細く）。麻呂眉は淡い黄土色。
